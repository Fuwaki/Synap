[package]
name = "synap-desktop-linux"
version = "0.1.0"
edition = "2024"

[dependencies]
gtk = { package = "gtk4", version = "0.11.2", features = ["v4_22"] }
synap-core = { path = "../core", version = "0.1.0" }
